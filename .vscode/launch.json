{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug aria-shell",
            "preLaunchTask": "cargo build (aria-shell)",
            "program": "${workspaceFolder}/target/debug/aria-shell",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}
